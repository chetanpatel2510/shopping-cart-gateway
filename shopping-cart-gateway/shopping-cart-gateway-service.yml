apiVersion: v1
kind: Service
metadata:
  labels:
    app: shopping-cart-gateway
  name: shopping-cart-gateway-service
  namespace: default
spec:
  ports:
  -  port: 8080
     protocol: TCP
     targetPort: 8080
  selector:
    app: shopping-cart-gateway
  type: LoadBalancer

